#include <stdio.h>

void FindSaddlePoint(int a[][N], int m, int n);

int main(int argc, char const *argv[])
{
    int m, n;
    printf("Input m,n:\n");
    scanf("%d,%d", &m, &n);

    int arr[m][n] = {0};

    return 0;
}

void FindSaddlePoint(int a[][N], int m, int n)
{
}
